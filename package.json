{
  "name": "@careermentor/db-contract",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "migration:generate": "sequelize-cli migration:generate --migrations-path src/migrations",
    "seed:generate": "sequelize-cli seed:generate --seeders-path src/seeders",
    "seed": "sequelize-cli db:seed:all",
    "migrate": "sequelize-cli db:migrate",
    "undo": "sequelize-cli db:migrate:undo",
    "undo:all": "sequelize-cli db:migrate:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.3.1",
    "mysql2": "^3.17.2",
    "sequelize": "^6.37.7",
    "sequelize-cli": "^6.6.5"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
